using UnityEngine;
using System.Collections;

public class CircuitLightGen : MonoBehaviour {

	// TODO: Make it a queue of point lights that can be taken out with StartLightAt(position, target)
	// and that automatically get sent back into the queue when the target position is reached.
	// If a light is requested when the queue is empty, make a new one.

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
